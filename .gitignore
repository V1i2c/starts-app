node_modules
dist
dist-ssr
*.local

# Capacitor's native Android project is generated fresh by CI (see
# .github/workflows/android-build.yml) from capacitor.config.ts + android-native/,
# so it never needs to be committed — that also means it can't go stale.
/android
/ios

.DS_Store
*.log
